[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 600
startCommand = "sh -c 'npx medusa db:migrate && npm run start'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
